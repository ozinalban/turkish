set<string> fruitsSet =  new set<string>();
for(fruits: fruitsSet){
    sydtrm.debug(fruits);
}
system.debug('the size of the set is '+ fruitSets.size());
fruitSets.add('apricot');
system.debug('The new list '+ fruitSet);
system.debug(fruitSets.isEmpty());