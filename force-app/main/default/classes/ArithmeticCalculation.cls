public  class ArithmeticCalculation {
    public  integer addition(integer x, integer y ) {
        integer z = x+y;
        system.debug(z);
        return z;
    }

    public integer addition1(integer a, integer b, integer c, integer d){
        integer sumValue = a+b+c+d;
        system.debug('toplam '+ sumValue);
        return sumValue;
    }
}
